[pytest]
testpaths = tests
pythonpath = .
addopts = -q
markers =
    slow: exercises real Zarr I/O and dask execution. Skip during iteration
        with `pytest -m "not slow"`; CI always runs everything.
filterwarnings =
    ignore:Consolidated metadata is currently not part in the Zarr format 3:UserWarning
